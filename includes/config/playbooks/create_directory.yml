---
- hosts: localhost
  gather_facts: false
  become: yes
  tasks:
  - name: Make sure directory is present
    file:
      path: "{{ DIRECTORY }}"
      state: directory
      owner: "{{ lookup('env','USER') }}"
      group: "{{ lookup('env','MYGIDNAME') }}"