---
pgrole: 'calibre'
intport: '8080'
image: 'linuxserver/calibre:latest'
pg_volumes:
  - "{{ lookup('env','CONFDIR') }}/docker/{{user.name}}/{{pgrole}}/config:/config:rw"
  - "{{ lookup('env','HOME') }}:{{ lookup('env','HOME') }}"
  - '/etc/localtime:/etc/localtime:ro'
