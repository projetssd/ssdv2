---
- name: Set 'nom_court' variable
  set_fact:
    secret_connect: lookup('community.general.random_string', length=20)

- name: Set 'fqdn' variable
  set_fact:
    url_public: "http://{{ subdomain  }}.{{ user.domain }}"
