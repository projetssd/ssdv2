---
- name: Set 'nom_court' variable
  set_fact:
    fqdn: "{% is sub_enabled %}{{ sub.pgrole.pgrole }}.{{ user.domain}}"{% else %}{{ pgrole }}.{{ user. domain}}{% endif %}"


- name: Set 'fqdn' variable
  set_fact:
    fqdn: "{{ nom_court }}.{{ user.domain }}"

