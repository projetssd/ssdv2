---
- pause:
    prompt: |
      SSDV2_PROMPT {"id": "decypharrseed.login", "label": "Login", "kind": "text", "secret": false}
      Choisir votre login
    echo: yes
  register: login

- pause:
    prompt: |
      SSDV2_PROMPT {"id": "decypharrseed.password", "label": "Mot de passe", "kind": "secret", "secret": true}
      Choisir votre password
    echo: yes
  register: password
