---

- name: 'Stopping {{ pgrole }}'
  docker_container:
    name: '{{ pgrole }}'
    state: stopped

- name: 'Creating metube folder paths'
  become: yes
  file:
    path: "{{item}}"
    state: "directory"
    mode: "0777"
    owner: "{{user.userid}}"
    group: "{{user.groupid}}"
    recurse: "yes"
  with_items:
    - "{{ settings.storage }}/docker/{{ lookup('env','USER') }}/{{ pgrole }}/config"
    - "{{ lookup('env','HOME') }}/{{ pgrole }}/videos"
    - "{{ lookup('env','HOME') }}/{{ pgrole }}/audios"

- name: Restart {{ pgrole }}
  docker_container:
    name: '{{ pgrole }}'
    state: started
